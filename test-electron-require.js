try { const electron = require('electron'); console.log('electron typeof:', typeof electron, 'keys:', Object.keys(electron).slice(0,5)); } catch(e) { console.log('electron error:', e.message); } try { const ni = require('electron/js2c/node_init'); console.log('node_init typeof:', typeof ni); } catch(e) { console.log('node_init error:', e.message); } try { const bi = require('electron/js2c/browser_init'); console.log('browser_init typeof:', typeof bi); } catch(e) { console.log('browser_init error:', e.message); }
